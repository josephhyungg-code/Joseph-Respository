# Singapore Resale Watch — Saturday, 20 June 2026 (Daily digest)

## RESULT: 0 verified listings tonight

No listing could be **opened and verified live this run** from an actual source, so — per the two non-negotiable rules (real link only; meet ALL criteria) — **nothing is reported**. No near-misses, no triangulated snippets, no reused links, no fabricated entries.

## Hard filters applied (v2)
- Tenure: Freehold or 999-year only
- District: D15 (Joo Chiat / Katong / Siglap) or D19 (Kovan / Serangoon)
- Size: **strictly above** 1,000 sqft
- Price: SGD 2.0M – 2.2M
- PSF: **below** S$1,700
- Build year: 2000 or later
- Bedrooms: **more than 3** (i.e. 4+)
- Preferences (rank only, never reject): high floor, penthouse

## Output A — passing listing cards
*None. Zero listings cleared every hard filter with a live-verified URL this run.*

## Output B — price-change table

| project | unit | prev price | new price | Δ | link |
|---------|------|-----------|-----------|---|------|
| — | — | — | — | — | — |

No price changes could be computed: no current listing was verified live this run, so there is nothing to diff against the saved baseline.

## Why zero — access log (honest record)
- **PropertyGuru** — search *and* individual listing pages returned **HTTP 403 Forbidden** on live fetch this run.
- **99.co** — search *and* listing pages returned **HTTP 403 Forbidden** this run.
- **EdgeProp** — search *and* project/listing pages returned **HTTP 403 Forbidden** this run.
- **SRX** — condo pages returned **HTTP 403 Forbidden** this run.
- **Web search** surfaced only project/search *landing* pages and generic project blurbs — no individual listing whose price, size, PSF and bedroom count could be confirmed against a canonical, openable URL.
- The one concrete D19 freehold lead, **Bliss @ Kovan**, is confirmed to offer only **1- to 3-bedroom** units — it fails the new "more than 3 bedrooms" filter regardless of price, so it was rejected on that filter, not merely on access.

> The v2 rules explicitly forbid the triangulation-via-snippets approach used on the 6 Jun baseline run ("retrieved live this run from the actual source… if a URL cannot be opened/verified, drop that listing"). With every portal returning 403, the honest outcome is zero.

## Note on the saved baseline
The previous snapshot (2026-06-06) was taken under **looser** criteria. Under the current v2 hard filters — PSF **below 1,700** *and* **more than 3 bedrooms** — none of the three previously-saved "matches" would qualify today:
- The Line @ Tanjong Rhu — PSF $2,054 (over the 1,700 ceiling) and 3BR.
- Bliss @ Kovan — 2BR penthouse.
- Flamingo Valley — 2BR.

So even setting access aside, the saved baseline contains no v2-qualifying units to re-confirm.

## Suggested next step
Direct automated portal access is being blocked (403). To get reliable nightly data, consider an authenticated/API feed (e.g. a portal API key, an EdgeProp/SRX data subscription, or a logged-in session token) so listings can be fetched and their URLs verified live. Without that, nights like this will keep returning zero rather than risk fabricated links.
