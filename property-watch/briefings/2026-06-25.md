# Singapore Resale Watch — Thursday, 25 June 2026 (v2 criteria)

## RESULT: 0 listings pass — and 0 could be verified live this run

**No qualifying listing is reported tonight, because no listing URL could be retrieved and verified live from any of the three named sources.** Every direct request to PropertyGuru, 99.co and EdgeProp returned **HTTP 403 Forbidden** this run, across multiple districts and URL patterns. The v2 rules are explicit: every figure must carry a working URL retrieved live this run, and any listing whose URL cannot be opened must be dropped rather than shown. With zero verifiable URLs, the honest output is zero listings — not a padded one.

I did **not**:
- fabricate, guess, shorten, or reuse any URL;
- pad the list with near-misses;
- carry over the 6 June baseline units. (Those were captured under the older v1 criteria, can't be re-verified tonight, and all three fail the new v2 hard filters anyway — they were 2–3 bedroom and/or priced at $1,176–$2,054 psf, above the new $1,700 psf ceiling and below the new "more than 3 bedrooms" requirement.)

## v2 hard filters applied (all must pass; reject on first failure)
- Tenure: Freehold or 999-year only
- District: D15 (Joo Chiat / Katong / Siglap) or D19 (Kovan / Serangoon)
- Size: **strictly above** 1,000 sqft
- Price: SGD 2.0M – 2.2M
- PSF: **below** S$1,700 (hard ceiling)
- Build year: 2000 or later
- Bedrooms: **more than 3** (i.e. 4+)
- Preference only (not a filter): high floor, penthouse

## Output A — scannable cards
*None. No listing cleared retrieval + verification, so none can be shown.*

## Output B — price-change table
| project | unit | prev price | new price | Δ | link |
|---------|------|-----------|-----------|---|------|
| — | — | — | — | — | — |

*No verified listings this run, so no price changes can be computed against the saved snapshot.*

## Why zero — access note
Singapore's major portals actively block automated/server-side requests; all three returned 403 tonight (the same block the 6 June run hit, where figures were triangulated from third-party caches). Under v2's stricter "real link only" rule, triangulated-but-unverifiable figures are not allowed to appear as listings, so tonight returns clean-empty by design rather than reporting numbers I cannot stand behind.

**To get verified results,** retrieval needs to run from a context that can open the portals (authenticated session / official API / a residential-IP or browser-based fetch). Once any listing URL can be opened live, the same hard filters above will be applied and passing units will be appended to `property-watch/listings.csv`.

## CSV
Appended one audit row to `property-watch/listings.csv` recording this run as `NO_VERIFIED_LISTINGS` so the time-series shows the run happened (and why it was empty), with no fabricated data.
