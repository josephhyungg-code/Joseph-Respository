# Singapore Resale Watch — Sunday, 28 June 2026 (Daily Digest)

## RESULT: 0 verified listings tonight

**Zero listings can be shown tonight — not because nothing exists, but because nothing could be verified.** Every property portal (PropertyGuru, 99.co, EdgeProp), plus SRX and the Trovit aggregator, returned **HTTP 403 Forbidden** on every direct page fetch this run. Web *search* still works and surfaces project names and ballpark figures, but search snippets are not openable, verifiable listing pages — and the two non-negotiable rules require a **working URL retrieved and verified live this run** before any unit can appear. So every candidate was dropped rather than shown. No padding, no fabricated links.

This is not an organisation egress block: the proxy's own failure log is empty, which means the 403s come from the portals' anti-bot protection rejecting automated fetches — the same access wall noted on the 6 Jun run, now hitting the verification step directly.

## Output A — passing listing cards
*None.* No unit cleared the verification gate (a live, openable source URL), so none could be evaluated against the hard filters. A listing with an unverifiable link is a hard reject under the rules.

## Output B — price-change table

| project | unit | prev price | new price | Δ | link |
|---|---|---|---|---|---|
| _(none — no verified listings to compare)_ | | | | | |

## Comparison vs. saved repo baseline (2026-06-06)
- Nothing new or price-changed to report, because nothing could be verified live tonight.
- Separately worth flagging for future runs: the **v2 criteria added a hard "more than 3 bedrooms" (4BR+) filter.** All three units in the 6 Jun baseline — The Line @ Tanjong Rhu (3BR), Bliss @ Kovan (2BR penthouse), Flamingo Valley (2BR) — would now **fail** that filter even if re-verified. The effective target is now freehold/999-yr **4-bedroom+** units, >1,000 sqft, $2.0–2.2M, PSF < $1,700, built 2000+, in D15/D19. That is an extremely tight intersection (a 4BR above 1,000 sqft under $2.2M at sub-$1,700 psf implies a large, low-psf freehold unit), and is likely to return few or zero matches even on nights when the portals are reachable.

## What was attempted this run (access log)
- `WebSearch` across all three sources — **worked**, returned index snippets only.
- `WebFetch` (7 attempts), all **HTTP 403**:
  - EdgeProp D15 freehold search page
  - 99.co D19 freehold search page
  - PropertyGuru East Coast/Marine Parade D15 freehold search page
  - Trovit "freehold condo district kovan" aggregator
  - A specific PropertyGuru listing-detail page
  - SRX Suites @ Kovan project page
  - EdgeProp Kovan Apartments project page

## Recommendation
The blocker is portal anti-bot protection, not the search criteria. To get verified listings flowing, the most reliable fix is an authenticated/API-based feed for at least one source (e.g. an official portal API key or an authenticated MCP fetch tool) so listing pages can be opened and confirmed live. Until then, daily runs will keep returning 0 verified results whenever the portals serve 403 to automated fetches. Today's run is recorded in the snapshot and the append-only CSV so the time series stays intact.
