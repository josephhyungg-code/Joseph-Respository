# Singapore Resale Watch — Sunday, 5 July 2026

## RESULT: Zero verified listings tonight

**No listing could be shown, because none could be verified.** The two non-negotiable
rules require every listing to carry a working URL that is opened and verified live this
run from the actual source, and to drop any listing whose URL cannot be opened. This run,
**no listing URL could be opened or verified at all** — so, per the rules, nothing is shown
rather than padded with unverified data.

This is an environment access failure, not "there are no such units on the market." I am
stating it plainly instead of guessing.

## What was attempted

| Channel | Result |
|---|---|
| WebSearch (PropertyGuru / 99.co / EdgeProp queries) | Worked — returned indexed snippets and candidate URLs |
| WebFetch on portal listing/search pages | HTTP 403 Forbidden (every URL) |
| WebFetch on aggregators (stackedhomes) and a control page (Wikipedia) | HTTP 403 Forbidden — so verification is broken for all hosts, not just portals |
| curl direct (browser headers) to portals | Proxy egress blocked — `CONNECT tunnel failed, response 403` |
| curl to control host (example.com) | HTTP 000 — general curl egress is not open for non-allowlisted hosts |
| Agent proxy status | Healthy, no relay failures — the blocks are org egress policy + portal anti-bot, not a proxy fault |

Because I could not **open** a single candidate listing, I could not confirm any URL works,
confirm any price/size/PSF is current, or run the NEW / PRICE-CHANGED diff against the prior
snapshot. Presenting WebSearch snippets as "listings" would violate the real-verified-link
rule, so I did not.

## Output A — passing listings

None. Zero listings cleared verification tonight.

## Output B — price-change table

| project | unit | prev price | new price | Δ | link |
|---|---|---|---|---|---|
| _(none — no listing could be re-opened to compare)_ | | | | | |

## Note on the prior snapshot (2026-06-06)

Even setting the access block aside, note that the criteria tightened in v2 (PSF hard ceiling
now **below S$1,700**, and **≥ 3 bedrooms** required). Under those rules, none of the three
units from the 6 June baseline would qualify:

- **The Line @ Tanjong Rhu** — $2,054 psf, 3BR → fails PSF ceiling.
- **Bliss @ Kovan** — 2BR penthouse → fails the ≥3-bedroom rule.
- **Flamingo Valley** — 2BR → fails the ≥3-bedroom rule.

So the passing set is expected to be narrower going forward regardless of access.

## Time-series tracking

Today's run is recorded in `property-watch/listings.csv` (created this run) with status
`NO_VERIFIED_LISTINGS` so the gap is visible in the series rather than silently missing.

## To get real results next run

Verification needs a channel that can actually open portal pages. Options:
1. Allow-list `propertyguru.com.sg`, `99.co`, and `edgeprop.sg` in the session's egress
   policy so curl/WebFetch can reach them, or
2. Provide an authenticated data feed / API (e.g. an SRX or portal API key) the session can call, or
3. Run this digest from an environment where WebFetch is not returning 403 across the board.

Once any one of those is in place, this same routine will return verified cards.
