# Singapore Resale Watch — Tuesday, 7 July 2026

## BOTTOM LINE
**Zero listings pass tonight — and it's a data-access failure, not a market read.** Every source in the brief (PropertyGuru, 99.co, EdgeProp, plus SRX and PropNex) returned **HTTP 403** to automated retrieval on every page I tried — the district search pages *and* individual listing URLs, including the three units saved on 6 June. Because I could not open and verify a single live listing URL this run, the non-negotiable "real link only" rule means **no unit may be listed.** I have not fabricated, guessed, or reused an old link to pad the list.

## Criteria changed since the last run — worth flagging
The hard filters tightened versus the 6 June baseline. Two new gates now apply:
- **PSF must be BELOW S$1,700** (a hard ceiling that didn't exist on 6 June).
- **3 bedrooms or more** (6 June accepted 2BR units).

Consequence: **all three of the 6 June "top picks" would now be rejected even if re-verified** —
- *The Line @ Tanjong Rhu* — $2,054 psf → breaches the sub-$1,700 psf ceiling.
- *Bliss @ Kovan* — 2BR penthouse → fails the 3BR minimum.
- *Flamingo Valley* — 2BR → fails the 3BR minimum (and was already flagged as possibly stale).

So even setting the 403s aside, the previous shortlist does not survive the current brief. Tonight is a genuine reset.

## Output A — passing listings
**None.** No listing could be verified with a live, openable URL this run.

## Output B — price-change table
| project | unit | prev price | new price | Δ | link |
|---|---|---|---|---|---|
| *(none)* | — | — | — | — | — |

No price-change diff is possible: re-verifying the prior units requires opening their listing URLs, which returned 403 tonight.

## Why nothing is listed (data hygiene)
- **403 across the board.** PropertyGuru, 99.co, EdgeProp, SRX and PropNex all refused automated access (search pages and listing pages alike). The same wall was hit on 6 June; that run triangulated from indexed search snippets, but snippet text is not an opened-and-verified listing URL, so under the strict real-link rule those figures don't clear the bar this run.
- **The proxy is not the problem.** The egress proxy reported healthy with no relay failures — the 403s come from the portals' own bot protection, not an org policy block. Retrying the same way won't help.
- **What this is NOT.** This is *not* a finding that zero qualifying freehold 3BR units exist in D15/D19 at $2.0–2.2M under $1,700 psf. It only means none could be verified through automated retrieval tonight.

## The filter math (for context on how narrow this is)
To sit **below S$1,700 psf inside a S$2.0–2.2M budget**, a unit must be roughly **>1,180 sqft at $2.0M and >1,295 sqft at $2.2M**, be freehold/999-yr, 3BR+, built 2000+, in D15 or D19. In D15 that band is genuinely scarce — most freehold stock there clears $1,800–2,600 psf, so the sub-$1,700 ceiling excludes the bulk of it; the more realistic hunting ground is larger, older (2000s) freehold 3BRs and D19, where freehold PSF runs lower. Useful to know when the portals come back and the list can actually be built.

## How to get real results next run
The blocker is retrieval, not the market. Options:
1. Run the scan from an environment/session whose egress can reach the portals (or with portal API/authenticated access), so listing URLs actually open and can be verified per the rules.
2. Provide 2–3 candidate listing URLs directly and I'll verify each against every hard filter and diff against the saved snapshots.

Tonight's run is recorded (`snapshots/2026-07-07.json`) and the append-only time-series CSV was created (`listings.csv`) with a no-verified-matches row so the history stays continuous.
